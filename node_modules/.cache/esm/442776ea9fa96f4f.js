let User;_9fa‍.x([["connectToMongo",()=>connectToMongo],["handleStar",()=>handleStar],["checkIfUser",()=>checkIfUser],["userHasEnoughStars",()=>userHasEnoughStars],["handleTransaction",()=>handleTransaction]]);_9fa‍.w("./models/User",[["User",["User"],function(v){User=v}]]);const mongoose = require("mongoose");

let connectedToDB = false;

       function connectToMongo() {
	if (!connectedToDB) {
		mongoose.connect(process.env.MONGO_URI, {
			useNewUrlParser: true,
			useUnifiedTopology: true,
		});
		const db = mongoose.connection;
		db.on("error", _9fa‍.g.console.error.bind(_9fa‍.g.console, "connection error:"));
		db.once("open", function () {
			// we're connected!
			console.log("Connected to MongoDB");
		});
		connectedToDB = true;
	} else {
		console.log("Already connected");
	}
}

       function handleStar() {}

       function checkIfUser(username) {
	if (username[0] === "@") {
		username = username.substring(1);
	}
	if (username.length !== 11) {
		console.log("username format is wrong");
		return;
	}
	User.findOne({ username: username }).exec(function (err, user) {
		if (user) {
			return;
		} else {
			const newUser = new User({
				username: username,
				stars: 2,
				amountGiven: 0,
			});

			newUser.save((err) => {
				if (err) {
					_9fa‍.g.console.log(err);
				}
				console.log("User Created");
			});
		}
	});
}

       async function userHasEnoughStars(username, starsSent) {
	let foundUser = await User.findOne({ username: username });
	return !(foundUser.stars < starsSent);
}

function takeStars(username, amount) {
	User.findOne({ username: username }, (err, user) => {
		if (!user) {
			console.log("User doesn't exist. How'd you do this?");
		}
		user.stars -= amount;
		user.save();
	});
}

function giveStars(username, amount) {
	User.findOne({ username: username }, (err, user) => {
		if (!user) {
			console.log("User doesn't exist. How'd you do this?");
		}
		if (err) {
			_9fa‍.g.console.log(err);
			return;
		}
		_9fa‍.g.console.log(user.stars);
		// user.stars += amount;
		// user.save();
	});
}

       function handleTransaction(sender, receiver, starsSent) {
	takeStars(sender, starsSent);
	if (receiver.length > 1) {
		starsSent = starsSent / receiver.length;
	}
	for (let user of receiver) {
		checkIfUser(user);
		giveStars(user, starsSent);
	}
}
