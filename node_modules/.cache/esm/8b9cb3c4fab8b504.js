let showLeaderboard;_fab‍.x([["default",()=>handleMessage]]);_fab‍.w("./backend/main",[["showLeaderboard",["showLeaderboard"],function(v){showLeaderboard=v}]]);

               function handleMessage(message) {
	if (message === "!leaderboard") {
		showLeaderboard();
	}
}
