_966‍.x([["User",()=>User]]);const mongoose = require("mongoose");

const userSchema = new mongoose.Schema({
	username: String,
	stars: Number,
	amountGiven: Number,
});
       const User = mongoose.model("User", userSchema);
